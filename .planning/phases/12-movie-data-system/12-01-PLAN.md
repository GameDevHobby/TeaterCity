---
phase: 12-movie-data-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Scripts/data/MovieResource.gd
autonomous: true

must_haves:
  truths:
    - "MovieResource can be instantiated with all required fields"
    - "MovieResource serializes to dictionary with all fields"
    - "MovieResource deserializes from dictionary correctly"
  artifacts:
    - path: "Scripts/data/MovieResource.gd"
      provides: "Movie data model class"
      exports: ["MovieResource"]
      min_lines: 25
  key_links:
    - from: "MovieResource.to_dict()"
      to: "MovieResource.from_dict()"
      via: "dictionary round-trip"
      pattern: "to_dict.*from_dict"
---

<objective>
Create the MovieResource data class for storing movie information.

Purpose: Establish the movie data model (THTR-03) that will be used throughout the theater system for scheduling and display.
Output: Scripts/data/MovieResource.gd with all required fields and JSON serialization support.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase research
@.planning/phases/12-movie-data-system/12-RESEARCH.md

# Pattern reference
@Scripts/data/FurnitureResource.gd
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MovieResource data class</name>
  <files>Scripts/data/MovieResource.gd</files>
  <action>
Create MovieResource class following the FurnitureResource pattern:

```gdscript
class_name MovieResource
extends Resource

@export var id: String
@export var title: String
@export var genre: String
@export var rating: int = 50  # 0-100 scale (int to avoid JSON float issues)
@export var duration: int = 90  # Minutes (int to avoid JSON float issues)
```

Add `to_dict()` method returning Dictionary with all fields.
Add static `from_dict(data: Dictionary) -> MovieResource` method that:
- Creates new MovieResource
- Populates fields from dictionary with safe defaults
- Uses `data.get("field", default)` pattern for robustness

Safe defaults for from_dict:
- id: "" (empty string)
- title: "Unknown"
- genre: "Drama"
- rating: 50
- duration: 90

Do NOT add any scene references, visual components, or placement logic - this is pure data.
  </action>
  <verify>
Open the script in Godot editor (godot --editor --path .) and verify:
- No syntax errors
- Class registers as MovieResource
- All @export vars appear in inspector when creating a new resource
  </verify>
  <done>
MovieResource exists with id, title, genre, rating (int), duration (int) fields. Both to_dict() and from_dict() methods are implemented and can round-trip data.
  </done>
</task>

</tasks>

<verification>
1. Script loads without errors in Godot
2. Can create MovieResource.new() in GDScript
3. to_dict() returns dictionary with all 5 fields
4. from_dict(to_dict()) returns equivalent MovieResource
</verification>

<success_criteria>
- [ ] MovieResource.gd exists at Scripts/data/MovieResource.gd
- [ ] Class has id, title, genre, rating, duration fields
- [ ] rating and duration are int type (not float)
- [ ] to_dict() and from_dict() methods implemented
- [ ] No Godot parser errors when loading project
</success_criteria>

<output>
After completion, create `.planning/phases/12-movie-data-system/12-01-SUMMARY.md`
</output>
